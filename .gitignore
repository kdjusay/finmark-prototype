# === Global node_modules ignore ===
/frontend/node_modules/
/backend/node_modules/
/node_modules/   # In case someone installs in root by mistake

# === Frontend specific ignores ===
/frontend/build

# === Backend specific ignores ===
/backend/data/*.json  # Example: ignore generated data files (if applicable)
/backend/logs/        # Example: ignore log folders if used

# === General system & editor ignores ===
.DS_Store
Thumbs.db

# === Environment files ===
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# === Log files ===
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# === Optional coverage (if any test framework is added later) ===
/coverage

# === Optional pnp (if using Yarn Plug'n'Play, not common here) ===
/.pnp
.pnp.js